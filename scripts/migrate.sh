#!/bin/sh
# Миграции базы. Базы пока нет — заглушка держит место в конвейере прода,
# чтобы позже не переделывать порядок ступеней: тесты -> миграции -> деплой.
#
# Когда база появится: применять миграции здесь. Падать с ненулевым кодом,
# если хоть одна не применилась, — тогда деплой не запустится.
set -eu

echo 'Миграций нет: базы пока нет.'
